# /ernest-watch

Use the `ernest-watch` skill and, when useful, delegate to the `ernest-watch` subagent.

Run all enabled concerns from `memory/standing-concerns.md`.

Rules:

- Detect and remind only.
- Write reminder cards with `draft_trigger: "draft these"`.
- Never draft external content.
- Never mutate live systems.
- Return `[SILENT]` when clean.
