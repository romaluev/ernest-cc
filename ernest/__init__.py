"""Ernest engine: deterministic local-first operating layer for a CEO.

The engine produces watch cards, daily briefs, and draft-only outreach from
local data (or, when wired, from connectors via the VPS brain). It is pure
standard library so it runs anywhere Python 3.9+ is available, with no install
step beyond copying the package.
"""

__all__ = ["__version__"]
__version__ = "1.0.0"
